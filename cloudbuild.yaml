# cloudbuild.yaml
steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/spoonit-backend:$COMMIT_SHA'
      - '.'

images:
  - 'gcr.io/$PROJECT_ID/spoonit-backend:$COMMIT_SHA'
